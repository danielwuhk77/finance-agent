# clasp 的 .claspignore 是相对 rootDir(apps-script/)解析的。
# rootDir 之外的文件 clasp 本来就不会推,所以这里只需列 apps-script/ 下要忽略的项。
# 当前 apps-script/ 下没有要排除的文件,留空白让 clasp 推所有 .gs / .json / .html。

# 双保险:即使 clasp 不止扫 rootDir(老版本行为),也排除这些
../node_modules/**
../.git/**
../docs/**
../samples/**
../tests/**
../prompts/**
../schemas/**
../cloud-function/**
../*.md
../package*.json
../.env
../.env.*
